# re-wear

This site uses Astro.

To run the site in codespace, open the terminal and run the following command:

```bash
npm run dev
```

If that doesn't work, try running the install command first:

```bash
npm i
```

To close the server, go into the terminal and press `Ctrl+C`